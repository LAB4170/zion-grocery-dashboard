// Users routes removed - JWT authentication disabled
// System now uses simple frontend authentication with ZionGroceries/Zion123$ only

const express = require('express');
const router = express.Router();

// No user management routes needed for simple authentication
module.exports = router;
