// JWT Authentication middleware removed
// System now uses simple frontend authentication with ZionGroceries/Zion123$ only
// No backend authentication required

module.exports = {
  // Placeholder - no authentication middleware needed
};
